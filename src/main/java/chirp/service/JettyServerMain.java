package chirp.service;

/*
import org.eclipse.jetty.server.Server;
import org.glassfish.jersey.jetty.JettyHttpContainerFactory;
import org.glassfish.jersey.server.ResourceConfig;

import java.net.URI;
*/

public class JettyServerMain {

/*
  public static void main(String...args) throws Exception {
    URI baseUri = URI.create(ServerUtils.SERVER_BIND_ADDRESS);
    Server server = JettyHttpContainerFactory.createServer(baseUri, new ChirpApplication());

		// Preload data into the database.
    ServerUtils.resetAndSeedRepository();

		// Wait for shutdown ...
    System.out.format("Jetty HTTP Server started with WADL available at %s\nHit enter to stop it...\n\n", ServerUtils.WADL_RESOURCE);
    System.in.read();
  }
*/
}
